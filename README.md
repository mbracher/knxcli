# knxcli

knxcli could become a command-line toolkit for inspecting and controlling KNX installations from a terminal.
It might provide a small, scriptable interface for common bus tasks such as discovering devices, reading group addresses, and sending simple commands.
